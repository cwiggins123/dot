#!/bin/sh

mkdir -p "$HOME/.local/bin/"
ln -sf "$PWD/255color" "$HOME/.local/bin/"
ln -sf "$PWD/asciisampler" "$HOME/.local/bin/"
ln -sf "$PWD/aln" "$HOME/.local/bin/"
ln -sf "$PWD/bannerfish" "$HOME/.local/bin/"
ln -sf "$PWD/cheat" "$HOME/.local/bin/"
ln -sf "$PWD/clean-deps" "$HOME/.local/bin/"
ln -sf "$PWD/dmenuunicode" "$HOME/.local/bin/"
ln -sf "$PWD/duck" "$HOME/.local/bin/"
ln -sf "$PWD/doh" "$HOME/.local/bin/"
ln -sf "$PWD/eject" "$HOME/.local/bin/"
ln -sf "$PWD/ex" "$HOME/.local/bin/"
ln -sf "$PWD/fishies" "$HOME/.local/bin/"
ln -sf "$PWD/github-cloc" "$HOME/.local/bin/"
ln -sf "$PWD/lynx" "$HOME/.local/bin/"
ln -sf "$PWD/lynxa" "$HOME/.local/bin/"
ln -sf "$PWD/lynxar" "$HOME/.local/bin/"
ln -sf "$PWD/lynxlast" "$HOME/.local/bin/"
ln -sf "$PWD/watchcow" "$HOME/.local/bin/"
ln -sf "$PWD/urlencode" "$HOME/.local/bin/"
ln -sf "$PWD/emoji" "$HOME/.local/share/"
ln -sf "$PWD/fontawesome" "$HOME/.local/share/"
